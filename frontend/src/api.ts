/**
 * API module - re-exports from @/api for backward compatibility.
 *
 * New code should import from '@/api' (the api/ directory).
 * This file exists to maintain existing imports that use '@/api.ts'.
 */

export * from "./api/index";
